#include <bits/stdc++.h>

using namespace std;
#define FASTIO cin.tie(0);ios::sync_with_stdio(false);

int main(){
    FASTIO
    int n; cin >> n;
    if(n%2==1) cout << "SK";
    else cout << "CY";
    return 0;
}

