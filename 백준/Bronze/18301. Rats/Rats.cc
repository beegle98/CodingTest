#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<ll> vl;
typedef vector<int> vi;



int main(){
    int a,b,c; cin >> a >> b >> c;
    cout << (a+1)*(b+1)/(c+1)-1;

    return 0;
}

